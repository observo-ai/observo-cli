# Build output
/observo
/observo.exe
/dist/

# Goreleaser snapshots
*.tar.gz
*.zip
checksums.txt

# IDE
.idea/
.vscode/
*.swp

# Local env / secrets — never commit
.env
.env.local

# npm wrapper build artifacts
#   npm/bin/observo is the COMMITTED launcher shim and MUST ship in the
#   tarball (it is what npm symlinks into node_modules/.bin). Only the
#   native binary, downloaded per-platform at install time, is ignored.
/npm/bin/observo-bin
/npm/bin/observo-bin.exe
/npm/node_modules/

# OS noise
.DS_Store
Thumbs.db
